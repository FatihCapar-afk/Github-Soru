Soru 1 (if-else koşul yapısının ve değişken değerinin nasıl güncellendiğini test ediyor.)
What will be the value of grade after the following code executes?
grade = 65;
if (grade > 50)
{ grade = grade + 10; }
else
{ grade = grade - 10; }
----------------------------------------------------------------------------------------
Soru 2 (Çoklu if - else if - else bloklarında kodun hangi şarta uyup çalışacağını gösteriyor.)
Given the following pseudo code, what will be the output?
cgpa = 3.5;
if (cgpa > 3.0)
{ print("High Honor") }
else if ( cgpa < 2.0 )
{ print("Probation") }
else
{ print("Satisfactory") }

Cevap: High Honor
----------------------------------------------------------------------------------------
Soru 3 (switch-case yapısının mantığını ve eşleşen değere gitmeyi öğretiyor.)
What will be the output of this switch-case pseudo code?
level = 2;
switch(level)
{   case 1:
print("Freshman");
break;
case 2:
print("Sophomore");
break;
case 3:
print("Junior");
break;
default:
print("Senior/Other");
}

Cevap: Sophomore
----------------------------------------------------------------------------------------
Soru 4 (while döngüsünün, içindeki şarta bağlı olarak kaç kez döneceğini hesaplatıyor.)
How many times will the following while loop print "Registered"?
capacity = 30;
while(capacity > 25) {
print("Registered");
capacity = capacity - 1;
}

Cevap: 5 
----------------------------------------------------------------------------------------
Soru 5 ("Eşit değildir" (!=) mantıksal operatörünün kodlamadaki doğru yazımını test ediyor.)
Which of the following is the correct way to check if a variable studentCount is not equal to 100 in languages like C/C++, Java and Python.

Cevap: if(studentCount != 100)
----------------------------------------------------------------------------------------
Soru 6 (Değer atama (=) ile eşitlik kontrolü (==) arasındaki hayati farkı kavratıyor.)
What will be the value of x after executing the following pseudo codes
a)
x = 10;
x == 20;
b)
x = 10;
x = 15;

Cevap: 10, 15
----------------------------------------------------------------------------------------
Soru 7 (Dizilerin (array) 0. indeksten başladığı kuralını (sıfır tabanlı indeksleme) hatırlatıyor.)
If a zero-indexed array has N elements, what will be the index of its last element?

Cevap: N - 1
----------------------------------------------------------------------------------------
Soru 8 (Mantıksal VE (&&) operatörünün iki koşul da doğru olduğunda nasıl çalıştığını gösteriyor.)
What is the result of the following expression?
Here && is logical AND operator
( 12 < 15 ) && ( 8 > 5 )

Cevap: True
----------------------------------------------------------------------------------------
Soru 9 (Kod içinde temel matematiksel işlemlerin (çarpma, toplama) ve atamaların işleyişini test ediyor.)
What will be the value of finalScore after the following code?
midterm = 40;
project = 20;
finalScore = midterm * 2 + project;

Cevap: 100
----------------------------------------------------------------------------------------
Soru 10 (Mantıksal VEYA (||) operatörünün koşullardan biri doğru olduğunda nasıl sonuç verdiğini öğretiyor.)
What will be the output of the following code?
( Where, || is a logical operator )
absence = 5;
score = 45;
if(absence > 4 || score < 50) {
print("Retake Course");
} else {
print("Pass Course");
}

Cevap: Retake Course
----------------------------------------------------------------------------------------
Soru 11 (Mod (%) ve tam sayı bölmesi (/) kullanarak bir sayının basamaklarına ayrılması algoritmasını gösteriyor.)
Which of the following pseudo code will correctly print the digits of a given student ID 'stdId' in reverse order?

Cevap:
while( stdId > 0)
{
print (stdId % 10)
stdId = stdId / 10
}
----------------------------------------------------------------------------------------
Soru 12 (Bir dizi (array) içindeki verilere indeks numaralarıyla (örn: scores[1]) nasıl erişileceğini test ediyor.)
For a given array
scores[] = {45, 78, 92, 60, 85};
total = scores[1] + scores[4]
what will be the value of total?

Cevap: 163
----------------------------------------------------------------------------------------
Soru 13 (İç içe döngülerin (nested loops) çarpılarak toplamda kaç kez işlem yapacağını hesaplatıyor.)
How many times will the following code execute the print statement?
for( i = 1 to 4) {
for( j = 1 to 5) {
print(i, j);
}
}

Cevap: 20
----------------------------------------------------------------------------------------
Soru 14 (İkili (binary) sayı sistemindeki bir değeri, onluk (decimal) sayı sistemine çevirmeyi test ediyor.)
What integer is represented as 11011 in binary?

Cevap: 27
----------------------------------------------------------------------------------------
Soru 15 (Onluk (decimal) sayı sistemindeki bir değeri, ikili (binary) formata dönüştürmeyi öğretiyor.)
What is the binary representation of 52?

Cevap: 110100